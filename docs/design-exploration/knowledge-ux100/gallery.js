fetch('./screenshots.json').then(r=>r.json()).then(shots=>{function render(){const box=document.getElementById('gallery');box.replaceChildren();for(const s of shots.filter(s=>(!filter.value||s.direction===filter.value)&&(!width.value||String(s.width)===width.value)&&(!themeFilter.value||s.theme===themeFilter.value))){const f=document.createElement('figure'),a=document.createElement('a'),img=document.createElement('img'),c=document.createElement('figcaption');c.textContent=`${s.direction.toUpperCase()} · ${s.state} · ${s.width}px · ${s.theme}`;img.src='./screenshots/'+s.file;img.alt=c.textContent;img.loading='lazy';a.href=img.src;a.append(img);f.append(a,c);box.append(f);}}for(const id of ['filter','width','themeFilter'])document.getElementById(id).onchange=render;render();});
