/* Isolated UX adapter. Canonical retrieval, lazy-body validation and calculations remain unchanged. */
(() => {
 'use strict';
 const BASE='/docs/design-exploration/knowledge-ux100/',p=new URLSearchParams(location.search),$=id=>document.getElementById(id);
 const finalA=!p.has('d')||p.get('d')==='final',direction=['a','b','c'].includes(p.get('d'))?p.get('d'):'a',variant=finalA?'final':direction,demo=p.get('state')||'landing';
 document.body.classList.remove('direction-a');document.body.classList.add('direction-'+direction);document.body.classList.toggle('light-theme',p.get('theme')==='light');
 if(finalA){
  document.body.classList.add('final-a');
  $('question').placeholder='Fråga om investeringar…';
  $('question').setAttribute('enterkeyhint','send');
  const submit=$('composer').querySelector('button');
  submit.replaceChildren();submit.setAttribute('aria-label','Skicka fråga');submit.title='Skicka fråga · Enter';
  const icon=document.createElementNS('http://www.w3.org/2000/svg','svg');icon.setAttribute('viewBox','0 0 24 24');icon.setAttribute('width','24');icon.setAttribute('height','24');icon.setAttribute('aria-hidden','true');icon.setAttribute('focusable','false');
  const path=document.createElementNS(icon.namespaceURI,'path');path.setAttribute('d','M12 19V5M5 12l7-7 7 7');path.setAttribute('fill','none');path.setAttribute('stroke','currentColor');path.setAttribute('stroke-width','1.8');path.setAttribute('stroke-linecap','round');path.setAttribute('stroke-linejoin','round');icon.append(path);submit.append(icon);
  $('moreSearch').remove();
 }
 $('theme').onclick=()=>document.body.classList.toggle('light-theme');$('menu').onclick=()=>{const open=$('menu').getAttribute('aria-expanded')!=='true';$('menu').setAttribute('aria-expanded',String(open));$('globalNav').classList.toggle('open',open);};
 document.addEventListener('keydown',e=>{if(e.key==='Escape'&&$('globalNav').classList.contains('open')){$('menu').click();$('menu').focus();}});
 let K=window.NTMKnowledgeCatalog,sequence=0,trail=[],currentId=null,searchPage=0;
 const n=(tag,text,cls)=>{const el=document.createElement(tag);if(text!==undefined)el.textContent=text;if(cls)el.className=cls;return el;};
 const button=(text,fn,cls='text-button')=>{const b=n('button',text,cls);b.type='button';b.onclick=fn;return b;};
 const a=(text,url)=>{const el=n('a',text);el.href=url;return el;};
 const setStatus=text=>$('status').textContent=text;
 const warning=text=>{const el=n('p',text,'fixture-note');$('main').prepend(el);};
 if(['suppressed','load-error','version-error','index-error'].includes(demo))warning('Granskningsläge: avsiktlig testfixture för ett fel eller utgånget innehåll. Inte ett påstående om driftläget.');
 if(demo==='suppressed'&&K)K=window.NTMKnowledgeCore.create({...K,entries:K.entries.map(e=>e.id==='isk-year-scope'?{...e,reviewDue:'2000-01-01'}:e)});
 if(['load-error','version-error'].includes(demo)){
  const original=window.fetch.bind(window);window.fetch=async(...args)=>{if(String(args[0]).includes('data/knowledge/answers/pe.json')){if(demo==='load-error')return new Response('',{status:503});const response=await original(...args),body=await response.json();body.contentVersion=-1;return new Response(JSON.stringify(body),{status:200});}return original(...args);};
 }
 if(!K||demo==='index-error'){$('ask').replaceChildren(n('h1','Kunskapsbanken kunde inte öppnas'),n('p','Läs ett granskat svar direkt eller försök att ladda sidan igen.'),a('Läs om P/E','/fragor-svar-pe-tal.html'),button('Försök igen',()=>location.assign(BASE+'app.html?d='+variant)));return;}
 const entry=id=>K.entries.find(e=>e.id===id),safeEntry=id=>K.publicEntries().find(e=>e.id===id);
 const labels={definition:'Förklara',comparison:'Jämför',interpretation:'Tolka',calculation:'Räkna',limitation:'Begränsningar',misconception:'Vanliga missförstånd',process:'Arbetssätt',application:'Använd i praktiken'};
 let actionPromise;
 const actions=()=>actionPromise||(actionPromise=fetch(BASE+'actions.json',{credentials:'omit'}).then(r=>{if(!r.ok)throw Error();return r.json();}));
 function url(id,mode='ask'){const u=new URL(BASE+'app.html',location.origin);u.searchParams.set('d',variant);u.searchParams.set('theme',document.body.classList.contains('light-theme')?'light':'dark');if(id&&safeEntry(id))u.searchParams.set('id',id);if(mode!=='ask')u.searchParams.set('mode',mode);if(contextName)u.searchParams.set('context',contextName);return u.pathname+u.search;}
 function remember(id,mode='ask'){history.pushState({id:id||null,mode,trail:trail.slice(-5)},'',url(id,mode));}
 function mode(name,save=true){if(!['ask','search','explore'].includes(name))name='ask';sequence++;for(const m of ['ask','search','explore']){$(m).hidden=m!==name;const b=document.querySelector(`[data-mode="${m}"]`);if(m===name)b.setAttribute('aria-current','page');else b.removeAttribute('aria-current');}if(save)remember(null,name);if(name==='search'){renderSearch();$('searchInput').focus();}else if(name==='explore')renderCategories();}
 document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{mode(b.dataset.mode);if(b.dataset.mode==='ask')$('question').focus();});
 function renderTrail(){const el=$('trail');el.replaceChildren();if(direction==='c'||trail.length<2)return;el.append(n('span',direction==='a'?'Din frågeresa':'Du utforskar','eyebrow'));for(const id of trail.slice(-5)){const e=entry(id);if(e)el.append(button(e.question,()=>show({kind:'ANSWER',answerId:id}),id===currentId?'trail-current':'text-button'));}}
 function disclosure(title,content,cls){const d=n('details',undefined,cls);d.append(n('summary',title),content);return d;}
 function related(e){const box=n('section',undefined,'related');box.append(n('h2','Fortsätt utforska'));
  const types={'prerequisite':'Förstå grunden','deeper':'Fördjupa','comparison':'Jämför med','common-confusion':'Vanlig förväxling','related-metric':'Närliggande begrepp','next-question':'Nästa fråga','application':'Använd i praktiken'};
  // The lazy body projection intentionally cannot resolve other IDs alone.
  // Typed cross-answer links belong to the complete compact index.
  const refs=entry(e.id)?.relatedAnswers||[];if(!refs.length)box.append(n('p','Fler granskade frågor finns i Utforska.'));
  for(const r of refs.slice(0,6)){const next=safeEntry(r.id);if(!next)continue;const link=a('',url(r.id));link.className='related-link';link.append(n('small',types[r.type]||'Fördjupa'),n('span',next.question+' ↗'));link.onclick=ev=>{if(ev.ctrlKey||ev.metaKey||ev.shiftKey)return;ev.preventDefault();show({kind:'ANSWER',answerId:r.id});};box.append(link);}return box;
 }
 async function use(e){const box=n('section',undefined,'use');box.append(n('h2','Använd det'));let rows=[];try{rows=(await actions())[e.id]||[];}catch(_){box.append(n('p','Fortsättningslänkarna kunde inte laddas.'));return box;}
  for(const r of rows.slice(0,4)){const target=new URL(r.url,location.origin);if(target.origin!==location.origin||!target.pathname.endsWith('.html'))continue;const label=(r.type==='learn'?'Träna i Academy: ':r.type==='research'?'Öppna ':r.type==='workflow'?'Använd i Research: ':r.type==='macro'?'Se i Makro: ':'Testa: ')+r.title;const link=a(label+' ↗',target.pathname+target.search+target.hash);link.target='_blank';link.rel='noopener noreferrer';link.dataset.product='';box.append(link);}
  box.append(n('p',rows.length?(finalA?'Öppnas i ny flik. Svaret är kvar här. Inga värden eller texter förs över.':'Öppnas i en ny flik. Svaret ligger kvar här. Inga värden eller texter överförs.'):'Ingen publicerad produktfortsättning är kopplad till detta svar.'));return box;
 }
 function trust(e){const list=n('div',undefined,'source-list');list.append(n('p','Senast granskad '+e.reviewedAt));const supports={shortAnswer:'Kort svar',fullAnswer:'Fördjupning',example:'Exempel',caveats:'Begränsningar',comparison:'Jämförelse'};
  for(const s of e.sources||[]){const row=n('section'),link=a(s.title,s.url);link.target='_blank';link.rel='noopener noreferrer';row.append(link,n('p',s.supports?.length?'Underlag för: '+s.supports.map(v=>supports[v]).filter(Boolean).join(', ')+'.':'Källan är angiven i svaret; avsnittsstöd är inte specificerat.'));if(s.reviewedAt)row.append(n('p','Granskningsdatum: '+s.reviewedAt));if(s.limitations)row.append(n('p',s.limitations));list.append(row);}
  list.append(n('p','Antalet källor är inte ett kvalitetsbetyg. Läs vad underlaget stödjer.'));const count=(e.sources||[]).length;const d=disclosure(`Källor & granskning · ${count} ${count===1?'källa':'källor'}`,list,'sources');d.id='sources';return d;
 }
 function comparison(c){const table=n('table',undefined,'comparison'),head=n('thead'),hr=n('tr'),body=n('tbody');table.append(n('caption','Jämförelse av begreppen'));for(const text of ['Aspekt',c.leftLabel,c.rightLabel]){const th=n('th',text);th.scope='col';hr.append(th);}head.append(hr);table.append(head);for(const x of c.axes){const row=n('tr'),th=n('th',x.label);th.scope='row';row.append(th,n('td',x.left),n('td',x.right));body.append(row);}table.append(body);return table;}
 function formula(){const box=n('div');box.append(n('p','Endast P/E. Pris och EPS ska ha samma valuta och aktiebas.'));
  const form=n('form');form.className='formula-form';for(const [name,text]of [['price','Pris per aktie'],['eps','EPS per aktie']]){const lab=n('label',text),input=n('input');input.type='number';input.step='any';input.name=name;input.required=true;lab.append(input);form.append(lab);}for(const [name,text,opts]of [['currency','Valuta',[['SEK','SEK'],['USD','USD'],['EUR','EUR'],['GBP','GBP']]],['period','EPS-period',[['annual','Helt år'],['ttm','Senaste tolv månaderna'],['forward','Kommande helt år – antagande']]]]){const lab=n('label',text),select=n('select');select.name=name;for(const [value,title]of opts){const o=n('option',title);o.value=value;select.append(o);}lab.append(select);form.append(lab);}const submit=n('button','Beräkna P/E');submit.type='submit';form.append(submit);form.onsubmit=ev=>{ev.preventDefault();const f=form.elements;show(K.calculate('pe',{price:Number(f.price.value),eps:Number(f.eps.value),currency:f.currency.value,period:f.period.value}));};box.append(form);return disclosure('Pröva P/E med egna exempelvärden',box,'formula');}
 async function show(outcome,save=true){mode('ask',false);const ticket=++sequence;currentId=null;$('answer').replaceChildren();$('starts').hidden=true;$('again').hidden=false;document.body.classList.add('has-answer');setStatus('');
  if(outcome.kind!=='ANSWER'){
   const view=n('section',undefined,'boundary'),copy={CLARIFY:['Menar du…','Välj den granskade fråga som passar det du vill förstå.'],INVALID_INPUT:['Lite mer underlag behövs','För P/E behövs pris, EPS, samma valuta och en helårsperiod. Använd fälten nedan.'],ABSTAIN_UNSUPPORTED_JUDGMENT:['Ett nyckeltal kan inte fatta ditt beslut','Kunskapsbanken avgör inte vad du bör köpa eller sälja. Du kan förstå metoden och pröva dina egna antaganden.'],ABSTAIN_NO_COVERAGE:['Här saknas ett granskat svar','Prova en tydligare fråga eller utforska kunskapsbanken. NTM fyller inte luckan med ett påhittat svar.']}[outcome.kind]||['Svaret är inte tillgängligt','Prova en annan fråga.'];
   const heading=n('h2',copy[0]);heading.tabIndex=-1;view.append(heading,n('p',copy[1]));if(outcome.kind==='CLARIFY')for(const id of outcome.choices){const e=safeEntry(id);if(e)view.append(button(e.question,()=>show({kind:'ANSWER',answerId:id}),'choice'));}
   if(outcome.kind==='INVALID_INPUT'){const f=formula();f.open=true;view.append(f);}if(outcome.kind==='ABSTAIN_UNSUPPORTED_JUDGMENT'){view.append(button(entry('pe-interpretation').question,()=>show({kind:'ANSWER',answerId:'pe-interpretation'}),'choice'));const link=a('Bygg en egen tes i Research ↗','/research.html');link.target='_blank';link.rel='noopener noreferrer';view.append(link,n('p','Research öppnas separat. Ingen tes skrivs åt dig.'));}if(demo==='followup')view.append(button('Vad är forward P/E?',()=>ask('Vad är forward P/E?'),'choice'));view.append(button('Ställ en annan fråga',()=>$('question').focus()));$('answer').append(view);if(save){const kind={CLARIFY:'clarify',INVALID_INPUT:'invalid',ABSTAIN_UNSUPPORTED_JUDGMENT:'judgment',ABSTAIN_NO_COVERAGE:'none'}[outcome.kind]||'none';history.pushState({mode:'ask',kind,choices:outcome.choices||[],trail:trail.slice(-5)},'',url(null)+'&outcome='+kind);}setStatus(copy[0]);heading.focus();renderTrail();return;
  }
  const meta=safeEntry(outcome.answerId);if(!meta){unavailable('Svaret väntar på granskning eller länken är okänd.');return;}
  setStatus('Öppnar granskat svar…');let e;try{e=await K.load(meta.id);if(!await K.explain(meta.id,'knowledge','compact'))throw Error();}catch(_){if(ticket===sequence)unavailable('Svaret kunde inte laddas säkert. Inget ofullständigt eller felaktigt versionsunderlag visas.',meta.id);return;}if(ticket!==sequence)return;
  currentId=e.id;if(trail.at(-1)!==e.id)trail.push(e.id);trail=trail.slice(-5);if(save)remember(e.id);renderTrail();
  const grid=n('div',undefined,'answer-grid'),article=n('article',undefined,'answer-reading'),aside=n('aside',undefined,'answer-aside');article.append(n('p',K.categories.find(c=>c.id===e.category)?.title||'Granskat svar','eyebrow'));const h=n('h2',e.question);h.tabIndex=-1;h.id='answerTitle';article.setAttribute('aria-labelledby',h.id);article.append(h,n('p',e.shortAnswer,'lead'));
  if(e.period&&e.period!=='timeless')article.append(n('p',`Gäller ${e.jurisdiction==='SE'?'Sverige · ':''}${e.period}`,'scope'));if(e.reviewDue&&e.reviewDue<=new Date().toISOString().slice(0,10))article.append(n('p','Ny granskning behövs. Läs med denna begränsning.','scope'));
  if(outcome.calculation){const c=outcome.calculation,calc=n('section',undefined,'calculated');calc.append(n('h3','Beräknat resultat'));if(c.value===null)calc.append(n('p','Negativ EPS — ingen vanlig P/E-tolkning.'));else{calc.append(n('p',Number(c.value.toPrecision(8))+' gånger','result-number'),n('p',`${c.inputs.price} ${c.inputs.currency}/aktie ÷ ${c.inputs.eps} ${c.inputs.currency}/aktie = ${Number(c.value.toPrecision(8))}`),n('p','Pris per aktie / vinst per aktie · '+{annual:'helt år',ttm:'senaste tolv månaderna',forward:'kommande helt år – antagande'}[c.inputs.period]));}calc.append(n('p','Beräkningen avgör inte om aktien är billig eller dyr. Exempelvärden sparas inte; en delad länk öppnar förklaringen.'));article.append(calc);}
  const depth=n('div',undefined,'depth-body');depth.append(n('p',e.fullAnswer));if(e.example)depth.append(n('h3','Exempel'),n('p',e.example));depth.append(n('h3','Begränsningar'),n('p',e.caveats));const d=disclosure('Fördjupa · exempel & begränsningar',depth,'depth');d.id='depth';article.append(d);
  if(e.sections?.comparison)article.append(comparison(e.sections.comparison),n('p',e.sections.comparison.limitations,'note'));
  if(e.sections?.formula?.template==='pe/1')article.append(formula());
  const source=trust(e),relations=related(e),product=await use(e);if(ticket!==sequence)return;
  const canonical=a('Läs den fristående svarssidan ↗','/'+K.url(e.id));canonical.target='_blank';canonical.rel='noopener noreferrer';
  if(direction==='b'){aside.append(source,relations,product,canonical);grid.append(article,aside);}else if(direction==='c'){
   const stages=n('nav',undefined,'answer-stages');stages.setAttribute('aria-label','Svaret på djupet');const panels=[n('section'),n('section'),n('section')];panels[0].append(d,relations);panels[1].append(source,canonical);panels[2].append(product);for(const [i,title]of ['Förstå mer','Granska underlaget','Använd det'].entries()){const b=button(title,()=>{panels.forEach((v,j)=>v.hidden=i!==j);stages.querySelectorAll('button').forEach((v,j)=>v.setAttribute('aria-expanded',String(i===j)));if(i===1)source.open=true;});b.setAttribute('aria-expanded',String(i===0));b.setAttribute('aria-controls','stage-'+i);panels[i].id='stage-'+i;panels[i].hidden=i!==0;stages.append(b);}article.append(stages,...panels);grid.append(article);
  }else{article.append(source,...(finalA?[product,relations]:[relations,product]),canonical);grid.append(article);}
  $('answer').append(grid);setStatus('Granskat svar öppnat.');if(demo==='deep'){d.open=true;source.open=true;}h.focus({preventScroll:true});
 }
 function unavailable(message,id){setStatus('Svaret är inte tillgängligt.');$('answer').replaceChildren(n('h2','Vi kunde inte öppna svaret'),n('p',message),button('Försök igen',()=>show({kind:'ANSWER',answerId:id||'pe'})),a('Till granskade svar','/fragor-svar.html'));}
 function ask(q){$('question').value=q;return show(K.respond(q));}
 $('composer').onsubmit=ev=>{ev.preventDefault();ask($('question').value);};document.querySelectorAll('[data-query]').forEach(b=>b.onclick=()=>ask(b.dataset.query));$('newQuestion').onclick=()=>{$('question').value='';$('question').focus();$('composer').scrollIntoView({block:'center',behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'});};
 function rows(entries,target){target.replaceChildren();for(const e of entries){const link=a('',url(e.id));link.className='result-row';link.append(n('small',(K.categories.find(c=>c.id===e.category)?.title||'')+' · '+labels[e.intent]),n('span',e.question+' ↗'));link.onclick=ev=>{if(ev.ctrlKey||ev.metaKey)return;ev.preventDefault();show({kind:'ANSWER',answerId:e.id});};target.append(link);}}
 function renderSearch(){
  if(finalA){
   const meaningful=window.NTMKnowledgeCore.normalize($('searchInput').value).length>=2;
   $('search').querySelector('.filters').hidden=!meaningful;
   let empty=$('searchBlank');if(!empty){empty=n('div',undefined,'search-blank');empty.id='searchBlank';empty.append(n('p','Sök efter en fråga eller ett begrepp, till exempel P/E.'),button('Vill du upptäcka något? Utforska →',()=>mode('explore')));$('results').before(empty);}empty.hidden=meaningful;
   if(!meaningful){$('results').replaceChildren();$('searchStatus').textContent=$('searchInput').value.trim()?'Skriv minst två bokstäver eller siffror för att söka.':'';return;}
   const normalized=window.NTMKnowledgeCore.normalize($('searchInput').value);
   // The core search supplies candidates/ranking. Recognized short acronyms
   // additionally use existing concept membership, so PE does not show CapEx
   // merely because a substring happens to match. No new aliases or ranking.
   const exactConcepts=normalized.length<=3&&!normalized.includes(' ')?K.knowledgeConcepts.filter(c=>[c.term,...(c.abbreviations||[])].some(t=>window.NTMKnowledgeCore.normalize(t)===normalized)).map(c=>c.id):[];
   const found=K.search($('searchInput').value,$('category').value).filter(e=>(!exactConcepts.length||e.conceptRefs?.some(id=>exactConcepts.includes(id)))&&(!$('intent').value||e.intent===$('intent').value));
   rows(found.slice(0,12),$('results'));
   $('searchStatus').textContent=!found.length?'Inga granskade svar matchar. Prova ett annat begrepp.':found.length>12?`${found.length} matchande svar. De första 12 visas. Precisera sökningen eller välj område för att hitta rätt.`:`${found.length} matchande svar`;
   return;
  }
  const found=K.search($('searchInput').value,$('category').value).filter(e=>!$('intent').value||e.intent===$('intent').value);const from=searchPage*12;rows(found.slice(from,from+12),$('results'));$('searchStatus').textContent=found.length?`${from+1}–${Math.min(from+12,found.length)} av ${found.length} granskade svar`:'Inga granskade svar matchar. Prova ett kortare begrepp.';$('moreSearch').hidden=found.length<=12;$('moreSearch').textContent=from+12<found.length?'Nästa 12 svar →':'Till första sidan';$('moreSearch').onclick=()=>{searchPage=from+12<found.length?searchPage+1:0;renderSearch();$('searchStatus').tabIndex=-1;$('searchStatus').focus();};
 }
 for(const c of K.categories){const o=n('option',c.title);o.value=c.id;$('category').append(o);}for(const [id,title]of Object.entries(labels)){const o=n('option',title);o.value=id;$('intent').append(o);}for(const id of ['searchInput','category','intent'])$(id).addEventListener(id==='searchInput'?'input':'change',()=>{searchPage=0;renderSearch();});
 function renderCategories(){$('categories').replaceChildren();$('concepts').replaceChildren();for(const c of K.categories){const b=button(c.title,()=>renderConcepts(c.id),'category-row');b.append(n('span','Utforska →'));$('categories').append(b);}}
 function renderConcepts(category,offset=0){const box=$('concepts');box.replaceChildren();const title=n('h2',K.categories.find(c=>c.id===category).title);title.tabIndex=-1;box.append(title);const valid=new Set(K.publicEntries().filter(e=>e.category===category).flatMap(e=>e.conceptRefs||[])),concepts=K.knowledgeConcepts.filter(c=>valid.has(c.id));for(const c of concepts.slice(offset,offset+10)){const group=n('details'),s=n('summary',c.term),list=n('div');group.append(s,list);let page=0;const render=()=>{const found=K.publicEntries().filter(e=>e.conceptRefs?.includes(c.id));rows(found.slice(page*8,page*8+8),list);if(found.length>8)list.append(button(page*8+8<found.length?'Fler frågor →':'Till första frågorna',()=>{page=page*8+8<found.length?page+1:0;render();}));};group.ontoggle=()=>{if(group.open)render();else list.replaceChildren();};box.append(group);}if(concepts.length>10)box.append(button(offset+10<concepts.length?'Fler begrepp →':'Till första begreppen',()=>renderConcepts(category,offset+10<concepts.length?offset+10:0)));title.focus();}
 const contexts={research:['Research','fcf','research.html'],calculator:['Aktievärdering','pe','aktievarderingskalkylator.html'],macro:['Makro','macro-releases','makro.html'],'public-report':['Offentlig analys','fcf','analys.html']};
 const contextName=Object.hasOwn(contexts,p.get('context'))?p.get('context'):Object.hasOwn(contexts,demo)?demo:null;
 let helpOrigin;
 if(contextName){const [title,id,path]=contexts[contextName],box=$('context');box.className='context-entry';box.append(n('p','Förklaring från '+title),n('p',contextName==='public-report'?'NTM:s begreppsförklaring är skild från författarens analys.':'Förstå begreppet utan att flytta ditt arbete.'));const b=button('Vad betyder '+(id==='fcf'?'FCF':id==='pe'?'P/E':'makrosläpp')+'?',async()=>{helpOrigin=b;$('helpTitle').textContent='Förklaring';$('helpBody').textContent='Öppnar granskat svar…';$('help').showModal();$('closeHelp').focus();try{const view=await K.explain(id,contextName==='public-report'?'public-report':contextName,'compact');if(!view)throw Error();$('helpTitle').textContent=view.question;$('helpBody').replaceChildren(n('p',view.excerpt),n('p',view.caveats),button('Fördjupa i Fråga NTM',()=>{$('help').close();show({kind:'ANSWER',answerId:id});}));}catch(_){$('helpBody').textContent='Förklaringen kunde inte öppnas. Ditt arbete är kvar.';}});box.append(b);const back=a('Öppna '+title+' i egen flik ↗','/'+path);back.target='_blank';back.rel='noopener noreferrer';box.append(back);}
 $('closeHelp').onclick=()=>$('help').close();$('help').addEventListener('close',()=>helpOrigin?.focus());
 window.addEventListener('popstate',ev=>{const v=ev.state||{};trail=(v.trail||[]).filter(id=>safeEntry(id)).slice(-5);$('question').value='';if(v.id||new URLSearchParams(location.search).get('id'))show({kind:'ANSWER',answerId:v.id||new URLSearchParams(location.search).get('id')},false);else if(v.kind)show({kind:{clarify:'CLARIFY',invalid:'INVALID_INPUT',judgment:'ABSTAIN_UNSUPPORTED_JUDGMENT',none:'ABSTAIN_NO_COVERAGE'}[v.kind],choices:(v.choices||[]).filter(id=>safeEntry(id))},false);else{mode(v.mode||'ask',false);if((v.mode||'ask')==='ask'){$('answer').replaceChildren();$('starts').hidden=false;$('again').hidden=true;$('trail').replaceChildren();setStatus('');document.body.classList.remove('has-answer');}}});
 window.addEventListener('pagehide',()=>{$('question').value='';$('searchInput').value='';document.querySelectorAll('.formula-form input').forEach(el=>el.value='');});
 history.replaceState({mode:p.get('mode')||'ask',trail:[]},'');
 const demos={pe:()=>ask('Vad är P/E?'),interpretation:()=>ask('Är P/E 40 dyrt?'),clarify:()=>ask('marginal'),comparison:()=>ask('P/E vs P/S'),calculation:()=>ask('Aktien kostar 100 SEK och årlig EPS är 5 SEK vad är P/E?'),invalid:()=>ask('Aktien kostar 100 och EPS är 5. Vad är P/E?'),negative:()=>show(K.calculate('pe',{price:100,eps:-5,currency:'SEK',period:'annual'})),none:()=>ask('Hur fungerar kvantdatorer?'),judgment:()=>ask('Är NVIDIA ett köp?'),deep:()=>show({kind:'ANSWER',answerId:'fcf-limitations'}),related:()=>show({kind:'ANSWER',answerId:'pe-interpretation'}),product:()=>show({kind:'ANSWER',answerId:'falsification'}),time:()=>show({kind:'ANSWER',answerId:'isk-year-scope'}),suppressed:()=>show({kind:'ANSWER',answerId:'isk-year-scope'}),'load-error':()=>show({kind:'ANSWER',answerId:'pe'}),'version-error':()=>show({kind:'ANSWER',answerId:'pe'}),malformed:()=>show({kind:'ANSWER',answerId:'not-an-answer'}),followup:()=>ask('Och forward då?'),search:()=>{mode('search',false);$('searchInput').value='P/E';renderSearch();},explore:()=>mode('explore',false)};
 demos['search-blank']=()=>mode('search',false);
 demos['search-pe']=()=>{mode('search',false);$('searchInput').value='PE';renderSearch();};
 if(p.has('id'))show({kind:'ANSWER',answerId:p.get('id')},false);else if(p.has('mode'))mode(p.get('mode'),false);else if(p.has('outcome'))show({kind:{invalid:'INVALID_INPUT',judgment:'ABSTAIN_UNSUPPORTED_JUDGMENT',none:'ABSTAIN_NO_COVERAGE',clarify:'CLARIFY'}[p.get('outcome')]||'ABSTAIN_NO_COVERAGE',choices:[]},false);else if(demos[demo])demos[demo]();
 // Read-only QA handle; no storage, telemetry, query retention or synthetic answer bodies.
 window.Exploration={get K(){return K;},show,mode,renderSearch,renderCategories,renderConcepts,get trail(){return trail.slice();},
  scale(count){if(![100,500,1200].includes(count))throw Error('Unsupported local scale fixture');const base=window.NTMKnowledgeCatalog,data=JSON.parse(JSON.stringify(base));const concepts=[...data.knowledgeConcepts];for(let i=100;i<count;i++){const seed=base.entries[i%100],id='scale-'+i,concept='scale-concept-'+Math.floor(i/4);if(!concepts.some(c=>c.id===concept))concepts.push({id:concept,term:'Skalprov '+Math.floor(i/4),category:seed.category,synonyms:[],abbreviations:[],related:[]});data.entries.push({...seed,id,slug:id,question:'Skalprov fråga '+i+'?',aliases:[],concepts:[concept],conceptRefs:[concept],relatedAnswers:[]});}data.knowledgeConcepts=concepts;K=window.NTMKnowledgeCore.create(data);return {count:K.entries.length,bytes:new TextEncoder().encode(JSON.stringify(data)).length};}
 };
})();
