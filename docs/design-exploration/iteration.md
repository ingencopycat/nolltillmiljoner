# Rendered critique → revisions

## Pass 1

63 Chromium captures at 1440 × 960 / 390 × 844; all themes/surfaces, thesis depth, public data, provenance. No horizontal page overflow or browser exceptions. Production hashes unchanged.

### A
The serif masthead and aligned financial abstract break from old NTM, but desktop chart evidence slips below the fold; mobile takes too long to reach history. Company/metrics have enough presence to survive a shorter opening. Public report reads well, but the owner control interrupts the reading path on mobile. Revision: tighten report opening, increase metadata legibility, move owner controls to the end of the reading flow on mobile; preserve the serif report personality.

### B
The chart work object is convincing and has fewer competing boxes than an admin template. However Public Analysis inherits too much private-workspace navigation and reads like an internal document. Revision: give public sharing a minimal masthead, narrower reading canvas and a right-side report index. Turn annual chart into a precise connected observation plot, retaining a zero baseline and discrete values. The chart is about annual observations, not a price history.

### C
The company/large-number pairing and deep green/pale mineral palette are recognisable, but the lower-right prose is filler and the vertical bars borrow A's structure. Revision: replace filler with an actual period ledger and make history horizontal rows with directly aligned annual values. Reduce the mass of the large-number region; preserve the identity pair, remove ornamental company punctuation. This direction should earn its distinctness through the data itself.

### Across all three
Mobile thesis review state is below the first screen. Add a compact due-state link near the title while keeping the case dominant. Source arrow controls need explicit metric-specific accessible names. Native search inputs consume Escape to clear search; add dialog Escape handling. Increase small metadata from 9–10px where practical. Use actual period source details on chart focus/click and a keyboard equivalent. Keep all changes inside the exploration directory.

## Adversarial verdict before revision

None resembles the old bordered dashboard at first glance. A risks looking like a newspaper; B risks generic productivity software; C risks branding outrunning research utility. Those risks guide revisions and remain owner trade-offs, not reasons to choose a winner. Real dense statements remain behind progressive disclosure rather than discarded. No fictitious prices, modeled returns, or invented finance values were added.

## Pass 2 outcome

63 further viewport captures plus 18 mobile full-page views. A now shows the full desktop annual plot and much more mobile evidence in the first viewport. B's public reading shell no longer includes the private rail; its annual chart uses distinct connected observations. C replaces the filler paragraph with period/source context and a directly labeled horizontal ledger; its desktop financial number no longer sits inside a large card. Mobile review state is visible near the title in all three.

Inspected the three overview identities, public mobile reading, thesis/deep state, provenance and the direct pre-Wave comparison. Remaining trade-offs are recorded in the direction report rather than concealed: A is comparatively editorial, B comparatively familiar software, C comparatively expansive. None is selected.

Final verification: no page exceptions, no page overflow in required sizes or 54 additional reflow combinations (360/430/720px), native dialog Escape and focus return, keyboard chart-source activation, hub navigation, in-memory editing/reset and no storage/backend writes. Palette text contrast ranges from 4.52:1 at the weakest secondary surface pairing to 15.39:1 for body text. This is not a full WCAG certification. See `qa/verification.json` and the per-pass `results.json` files.
