// Repository-safe example; keep disabled until the owner completes setup and verification.
// Replace the URL and public key with this project's public settings only.
window.NTMCloudConfig = Object.freeze({
  enabled: false,
  url: 'https://your-project-ref.supabase.co',
  publishableKey: 'sb_publishable_REPLACE_WITH_PUBLIC_KEY'
});
